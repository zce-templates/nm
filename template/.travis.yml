sudo: false

# os:
#   - linux
#   - osx
#   - windows

language: node_js

node_js:
  # - 8
  - lts/*

notifications:
  email:
    on_success: never
    on_failure: change

<% if (features.includes('coverage')) { %>
before_script:
  - yarn global add nyc codecov
<% } %>

script:
  - yarn lint
  - yarn test

<% if (features.includes('coverage')) { %>
after_script:
  - nyc report --reporter=text-lcov > coverage.lcov && codecov
<% } %>
